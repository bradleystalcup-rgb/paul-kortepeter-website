name = "paul-kortepeter-website"
pages_build_output_dir = "public"
compatibility_date = "2024-09-23"

[vars]
ADMIN_USERNAME = "paul"

# Created by `wrangler d1 create paul-kortepeter-db` — paste the resulting
# database_id below. See README.md for the full setup walkthrough.
[[d1_databases]]
binding = "DB"
database_name = "paul-kortepeter-db"
database_id = "REPLACE_WITH_D1_DATABASE_ID"
